{
  "name": "objects",
  "version": "0.0.1",
  "description": "Boilerplate",
  "main": "build/index.js",
  "author": "maciejcieslar",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "ndb .",
    "start:dev": "nodemon -e ts --exec \"yarn run build\"",
    "start:prod": "yarn run build",
    "build": "rm -rf build && tsc",
    "start": "rm -rf build && tsc && node build/index.js",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^23.3.9",
    "@types/module-alias": "^2.0.0",
    "@types/node": "^10.12.6",
    "jest": "^23.6.0",
    "ndb": "^1.0.26",
    "nodemon": "^1.18.6",
    "ts-jest": "^23.10.4",
    "tslint": "^5.11.0",
    "tslint-config-airbnb": "^5.11.0",
    "typescript": "^3.1.6"
  },
  "dependencies": {
    "module-alias": "^2.1.0"
  }
}
